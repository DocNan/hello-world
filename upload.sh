# upload test source codes to github
git add *
git commit . -m 'updata all files and cach in hello-world director'
# if you use "git commit * -m 'comments' ", sometimes there is still troubles!
# git push -u origin master
git push
